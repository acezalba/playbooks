- name: Install xcb-cursor
  become: true
  become_method: sudo
  ansible.builtin.apt:
    name: libxcb-cursor0
    state: present

- name: Install calibre using recommended install script
  shell: "sudo -v && wget -nv -O- https://download.calibre-ebook.com/linux-installer.sh | sudo sh /dev/stdin"